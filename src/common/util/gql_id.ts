// NOTE: Might be required for ids
//
// import crypto from "crypto";
//
// export default function gqlID(...values: string[]): string {
//   return crypto
//     .createHash("sha1")
//     .update(values.join("|"))
//     .digest("hex");
// }
